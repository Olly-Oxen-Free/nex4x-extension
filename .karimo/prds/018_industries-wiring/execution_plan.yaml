prd: industries-wiring
waves:
  - wave: 1
    tasks: [18a]                  # Constant — no deps; unblocks config and seeder
  - wave: 2
    tasks: [18b, 18e]             # Config file (depends 18a) + disruption fix (no deps) run in parallel
  - wave: 3
    tasks: [18c]                  # IndustrySeeder implementation — needs constant + config
  - wave: 4
    tasks: [18d, 18f]             # ModPlugin hook (depends 18c) + DebugCommand (depends 18c, 18e)
  - wave: 5
    tasks: [18g]                  # Build + smoke verify — all prior tasks complete
longest_chain: [18a, 18b, 18c, 18d, 18g]
total_complexity: 7
